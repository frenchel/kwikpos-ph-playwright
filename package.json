{
  "name": "kwikpos-ph-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:chrome": "npx playwright test --config=playwright.config.ts --project=chrome",
    "test:nav": "npx playwright test tests/navigation.spec.ts --project=chrome",
    "test:common": "npx playwright test tests/common-form.spec.ts --project=chrome --headed"
  },
  "author": "Frenchel Karylle B. Laroza",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
